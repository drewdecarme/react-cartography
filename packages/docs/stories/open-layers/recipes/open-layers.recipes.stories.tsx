import type { Meta } from "@storybook/react";

const meta: Meta = {
  title: "OpenLayers / Recipes",
} satisfies Meta<typeof meta>;

export default meta;

export const ComingSoon = () => <div>coming soon!</div>;
